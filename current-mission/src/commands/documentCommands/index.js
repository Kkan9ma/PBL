import { addImageFileCommand } from './addImageFileCommand';

export const documentCommandsMap = {
  addImageFile: addImageFileCommand,
};

export const documentCommandsValuesMap = {
  addImageFile: 'image',
};
